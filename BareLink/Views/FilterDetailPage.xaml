<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:BareLink.ViewModels" x:DataType="viewmodels:FilterDetailViewModel"
             x:Class="BareLink.Views.FilterDetailPage"
             Title="{Binding Name}">
  <ContentPage.ToolbarItems>
    <ToolbarItem IconImageSource="outline_edit_24.xml" Command="{Binding EditFilterCommand}"/>
    <ToolbarItem IconImageSource="outline_delete_24.xml" Command="{Binding DeleteFilterCommand}"/>
  </ContentPage.ToolbarItems>
  <StackLayout Spacing="20" Padding="15">
    <Label Text="{Binding Description}" FontSize="Large"/>
    <Label Text="RegEx pattern:" FontSize="Medium" />
    <Label Text="{Binding Pattern}" FontSize="Small" FontFamily="monospace"/>
    <StackLayout Orientation="Horizontal">
      <Label Text="Enabled" FontSize="Medium" VerticalTextAlignment="Center" HorizontalOptions="FillAndExpand" />
      <Switch IsToggled="{Binding Active, Mode=TwoWay}" HorizontalOptions="End" />
    </StackLayout>
  </StackLayout>
</ContentPage>